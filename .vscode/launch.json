{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch STM32 Debug Studio",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron.cmd",
      "args": [
        "."
      ],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "preLaunchTask": "Build desktop app"
    }
  ]
}